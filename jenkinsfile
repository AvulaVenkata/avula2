pipeline {
    agent any
    stages {

         stage('Test') {
            steps {
                sh 'echo "test application"'
            }
        }
       stage('Deploy') {
            steps {
                sh 'echo "Deploy application"'
            }
        } 
    }
}
